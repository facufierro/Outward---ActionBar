namespace fierrof.ActionBar
{
    public enum SlotMode
    {
        Active,   // Always visible, normal behavior
        Hidden,   // Visible only when inventory is open or item assigned; hidden otherwise
        Disabled  // Completely invisible and non-functional
    }
}
